"""Bridges that transport FM outputs into LLM-consumable artifacts.

Phase 3 fleshes out this subpackage with the language-anchored and
structure-preserving bridges, plus a shared base class that defines the
common contract. The package stays empty until then.
"""
